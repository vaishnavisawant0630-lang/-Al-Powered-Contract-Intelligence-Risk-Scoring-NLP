# ============================================================
# requirements-dev.txt — Development-only dependencies
# ============================================================
#
# INSTALLATION
# ------------
#   pip install -r requirements.txt -r requirements-dev.txt
#
# These packages are NOT installed in production (Docker image).
# They are only needed for local development and CI.

# [Testing]
pytest>=8.0.0,<9.0.0
pytest-cov>=4.1.0,<5.0.0        # Coverage reporting (--cov flag)
pytest-mock>=3.12.0,<4.0.0      # pytest fixtures for unittest.mock

# [Type checking]
mypy>=1.9.0,<2.0.0
types-Pillow>=10.0.0             # Stubs for Pillow type annotations
types-tqdm>=4.66.0               # Stubs for tqdm

# [Linting / Formatting]
ruff>=0.3.0,<1.0.0               # Fast linter + formatter (replaces flake8 + black)

# [Pre-commit]
pre-commit>=3.7.0,<4.0.0        # Git hooks for lint/format on commit

# [Notebooks (optional)]
jupyter>=1.0.0                   # For exploratory data analysis
